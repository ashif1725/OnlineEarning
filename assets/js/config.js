"use strict";

/*
 * SkillEarn Hub
 * Frontend Public Configuration
 *
 * IMPORTANT:
 * इस file में कोई secret, password, database credential,
 * JWT secret या private API key नहीं रखनी है।
 */

window.SKILLEARN_CONFIG = Object.freeze({
    appName: "SkillEarn Hub",

    // Production Backend API
    apiBaseUrl: "https://skillearnhub-1.onrender.com",

    // Current environment
    environment: "production"
});
